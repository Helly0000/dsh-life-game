# Bundle patch for dsh-life-game: mounts one row in the web profile roster.
# Applied as a profile bundle layer (the dsh.bundle.patch manifest field).
- insert:
    - id: life-game
      name: dsh-life-game
